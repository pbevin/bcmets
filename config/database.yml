# SQLite version 3.x
development:
  adapter: mysql
  database: bcmets_development
  user: pete
  socket: /tmp/mysql.sock

test: &TEST
  adapter: sqlite3
  database: /tmp/bcmets-test

production:
  adapter: mysql
  database: bcmets_rails
  user: pete
  socket: /var/lib/mysql/mysql.sock

cucumber:
  <<: *TEST
